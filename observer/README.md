# Observer Design Pattern

Useful when you need consistency of data between many objects. Eg. Model - View for UI design.
Also known as publish-subscribe (pub-sub) model. All observers are notified when there's a state change of the subject.