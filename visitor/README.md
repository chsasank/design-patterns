# Visitor Design Pattern

An operation that can be performed on a class (usually a composite) structure. Separates operations and its hierarchy from node hierarchy. Similar to strategy in that you can create different operations on that same structure.

## Where is this pattern used?

* Lark's visitor is exactly this pattern
* Compilers and interpreters on abstract syntax tree